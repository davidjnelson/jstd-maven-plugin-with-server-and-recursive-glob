jstd-maven-plugin-with-server-and-recursive-glob
================================================

An extension of the existing jstd-maven-plugin which supports running the server from maven, and running the client with recursive glob.